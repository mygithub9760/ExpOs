int main()
{
decl
	int temp, i, usertabledes;
	str uname;
enddecl

begin
	temp = exposcall("Read",-1,uname);
	temp = exposcall("Remusr",uname);
	write(temp);
	if (temp==0) then
		write("Success");
	endif;
	return 0;
end
}